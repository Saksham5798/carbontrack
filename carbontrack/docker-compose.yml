version: '3.8'

services:
  # --------------------------------------------------------
  # Frontend Service (Nginx)
  # --------------------------------------------------------
  frontend:
    image: ghcr.io/saksham5798/carbontrack-frontend:${TAG:-latest}
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: carbontrack-frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: always
    networks:
      - carbontrack-network

  # --------------------------------------------------------
  # Backend Service (Flask + Gunicorn)
  # --------------------------------------------------------
  backend:
    image: ghcr.io/saksham5798/carbontrack-backend:${TAG:-latest}
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: carbontrack-backend
    ports:
      - "5001:5000"
    environment:
      - FLASK_ENV=production
      - DB_HOST=database
      - DB_PORT=3306
      - DB_USER=carbontrack_user
      - DB_PASSWORD=YourStrongPassword@123
      - DB_NAME=carbontrack_db
      - JWT_SECRET=super_secret_production_key_123!
    depends_on:
      database:
        condition: service_healthy
    restart: always
    networks:
      - carbontrack-network

  # --------------------------------------------------------
  # Database Service (MySQL 8)
  # --------------------------------------------------------
  database:
    image: ghcr.io/saksham5798/carbontrack-db:${TAG:-latest}
    build:
      context: ./database
      dockerfile: Dockerfile
    command: --performance-schema=OFF --key-buffer-size=16M --max-connections=50
    container_name: carbontrack-db
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=RootPassword@123
      - MYSQL_DATABASE=carbontrack_db
      - MYSQL_USER=carbontrack_user
      - MYSQL_PASSWORD=YourStrongPassword@123
    volumes:
      - db_data:/var/lib/mysql
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - carbontrack-network

volumes:
  db_data:

networks:
  carbontrack-network:
    driver: bridge
